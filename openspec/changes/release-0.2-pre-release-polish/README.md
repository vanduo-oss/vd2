# release-0.2-pre-release-polish

Package vd2 for v0.2.0 release readiness: CHANGELOG, CONTRIBUTING, CI workflow, LICENSE, bundle-size reporter, OpenSpec folder. Update `package.json` to v0.2.0 and Node 24. Verify with the full validation pipeline and `pnpm pack --dry-run`. Prepare cross-repo reference updates in `framework/` and `docs/` on local dev branches (no pushes, no main edits).
