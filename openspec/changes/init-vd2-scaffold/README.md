# init-vd2-scaffold

Bootstrap the vd2 Vue 3 + Vite + vite-ssg project: package.json, tsconfig, vite.config.ts, .npmrc, ESLint/Stylelint, hello-world app that boots in dev, builds via vite-ssg, and serves from dist/.
