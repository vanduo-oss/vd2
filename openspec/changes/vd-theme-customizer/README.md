# vd-theme-customizer

P1-3 of the `full-capability-transfer` plan. Adds 2 documentation
pages (Theme switcher, Theme customizer) that demonstrate vd2's
existing `VdThemeSwitcher` and `VdThemeCustomizer` overlay
components. No new components.

See `proposal.md`, `design.md`, `tasks.md`, and
`specs/theme-customizer/spec.md`.
